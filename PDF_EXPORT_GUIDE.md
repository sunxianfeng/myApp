# PDF导出功能使用指南

## 功能概述

在题目集详情页面新增了"一键导出PDF"功能，可以将题目集中的所有题目导出为PDF文档。

## 使用方法

### 1. 导出PDF

1. 进入任意题目集详情页面：`/collections/[id]`
2. 点击页面右上角的"导出 PDF"按钮（带有下载图标）
3. 系统会在新窗口打开打印预览页面
4. 浏览器会自动弹出打印对话框
5. 在打印对话框中选择"另存为PDF"
6. 选择保存位置并保存

### 2. 打印预览页面功能

打印预览页面 (`/papers/print/[id]`) 包含以下内容：

- **试卷头部**：题目集标题、描述、题目数量、生成日期
- **题目列表**：所有题目按照添加时间排序
  - 题号
  - 题型标签（选择题、填空题、判断题、问答题等）
  - 难度级别
  - 题目内容
  - 选项（仅选择题和判断题显示）
  - 答题空间（填空题和问答题）
  - 备注信息
- **参考答案**：在新页面显示所有题目的答案和解析
- **页脚**：生成信息

### 3. 手动控制

在打印预览页面，你还可以：

- 点击"返回"按钮返回题目集页面
- 点击"打印 / 保存为 PDF"按钮手动触发打印

## 技术实现

### 前端实现

1. **打印预览页面**：`/app/(dashboard)/papers/print/[id]/page.tsx`
   - 纯净的打印布局，无导航栏和侧边栏
   - 自动触发 `window.print()`
   - 响应式设计，适配不同屏幕尺寸

2. **打印样式**：`/app/(dashboard)/papers/print/[id]/print.css`
   - 针对打印优化的CSS样式
   - 使用 `@media print` 媒体查询
   - A4纸张尺寸，合理的页边距
   - 避免内容被拆分到多页

3. **导出按钮**：在集合详情页添加
   - Neobrutalism设计风格
   - 醒目的位置（页面右上角）
   - 使用 `window.open()` 在新窗口打开

### 样式特性

- **Neobrutalism风格**：粗边框、强阴影、大胆的颜色
- **打印优化**：
  - 移除装饰性元素（阴影、渐变）
  - 保留关键的背景色（使用 `print-color-adjust: exact`）
  - 避免页面拆分（使用 `page-break-inside: avoid`）
  - 合理的页边距和行距
  - 清晰的层次结构

## 页面优化改进

### 1. UI布局优化

- **统计卡片**：新增题目总数、练习次数、平均掌握度三个统计卡片
- **控制栏改进**：
  - 中文化界面文字
  - 添加emoji图标
  - 改进筛选选项（显示星级）
  - 更好的视觉层次
- **空状态优化**：
  - 更友好的空状态提示
  - 大图标和清晰的文案
  - 引导按钮跳转到题目库

### 2. 加载和错误状态

- **加载状态**：精美的加载动画和卡片样式
- **错误状态**：友好的错误提示和返回按钮

### 3. 响应式设计

- 适配移动端和平板设备
- 弹性布局，自动调整间距
- 合理的断点和字体大小

## 浏览器兼容性

- ✅ Chrome / Edge (推荐)
- ✅ Firefox
- ✅ Safari
- ⚠️ 旧版浏览器可能需要手动触发打印

## 常见问题

### Q: 为什么PDF中没有背景色？

A: 某些浏览器默认不打印背景色。在打印对话框中，勾选"背景图形"或类似选项即可。

### Q: 如何调整PDF的页边距？

A: 在打印对话框中可以自定义页边距。CSS中默认设置为15mm。

### Q: 题目太长被拆分到两页怎么办？

A: 代码中已使用 `page-break-inside: avoid` 避免拆分，但如果单个题目内容过长，浏览器可能仍会拆分。

### Q: 可以只导出选中的题目吗？

A: 当前版本导出整个题目集。如需导出部分题目，可以先创建新的题目集并添加需要的题目。

## 未来改进方向

- [ ] 支持选择性导出（勾选题目）
- [ ] 自定义PDF样式（字体、颜色、布局）
- [ ] 批量导出多个题目集
- [ ] 导出为Word文档
- [ ] 后端生成PDF（更好的格式控制）

## 技术栈

- **前端框架**：Next.js 14 (App Router)
- **状态管理**：Redux Toolkit
- **样式方案**：CSS-in-JS + CSS Modules
- **打印方案**：浏览器原生 `window.print()` + CSS媒体查询

---

如有问题或建议，请联系开发团队。

